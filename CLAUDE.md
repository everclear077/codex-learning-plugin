# Claude Code contributor entrypoint

Read and follow [AGENTS.md](AGENTS.md) for repository scope, behavioral invariants, validation, privacy and Git rules. It is the shared source of truth; do not maintain a separate conflicting policy here.

This file guides contributors using Claude Code. It does not claim the Codex plugin manifest is a Claude Code plugin. Keep product behavior in `plugins/core-learning/skills/learn-core/` and use [the behavior scenarios](docs/behavior-evals.md) for tutoring changes.

Before proposing completion, run the commands listed in AGENTS.md and state which checks actually ran. Do not copy personal configuration into the repository or add an unrelated service/runtime.
